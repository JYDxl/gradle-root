spring:
  profiles:
    active: dev
  application:
    name: sub-web
  cloud:
    nacos:
      discovery:
        server-addr: ubuntu:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml
        group: SUB
