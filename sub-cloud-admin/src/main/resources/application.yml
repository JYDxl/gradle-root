management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

spring:
  application:
    name: admin-server
  cloud:
    nacos:
      discovery:
        server-addr: jydxl.link:8848
  thymeleaf:
    check-template-location: false

server:
  port: 9000

logging:
  file:
    path: logs/${spring.application.name}
  logback:
    rollingpolicy:
      max-file-size: 1GB
      max-history: 30
