spring:
  profiles:
    active: dev
  application:
    name: hub-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml
#        group: server

server:
  port: 8084
