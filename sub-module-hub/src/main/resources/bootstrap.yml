spring:
  profiles:
    active: dev
  application:
    name: hub-server
  cloud:
    nacos:
      discovery:
        server-addr: jydxl.link:8848,jydxl.link:8849,jydxl.link:8850
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml
#        group: server
        shared-configs:
          - data-id: j2cache-${spring.profiles.active}.properties

server:
  port: 8081
