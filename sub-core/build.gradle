dependencies {
  compile project(':sub-model')

  compileOnly('org.springframework.boot:spring-boot-starter-web') { exclude module: 'spring-boot-starter-tomcat' }
  compileOnly('javax.servlet:javax.servlet-api')
}
